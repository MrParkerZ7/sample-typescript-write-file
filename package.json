{
  "name": "sample-typescript-write-file",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "build": "node lib/index.js",
    "watch": "tsc -watch",
    "clean": "rimraf lib && rimraf output && rimraf node_modules",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/fs-extra": "^9.0.13",
    "@types/node": "^17.0.21",
    "typescript": "^4.6.2"
  },
  "dependencies": {
    "tslib": "^1",
    "fs-extra": "^10.0.1"
  }
}
